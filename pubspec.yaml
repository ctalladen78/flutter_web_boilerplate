name: flutter_web_app_1
description: An absolute bare-bones web app.
# version: 1.0.0
#homepage: https://www.example.com

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http:
  dio:
  rxdart:
  # mapbox_gl:
  flutter_dotenv:
  google_sign_in:
  firebase_core:
  firebase_auth:
  flutter_cache_manager:

dev_dependencies:
  build_runner: ^1.6.0
  build_web_compilers: ^2.3.0
  pedantic: ^1.8.0

assets:
  - dotenv